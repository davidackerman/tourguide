fastapi==0.115.4
uvicorn[standard]==0.32.0
websockets==13.1
slowapi==0.1.9
numpy==1.26.4
scipy==1.14.1
scikit-image==0.24.0
pandas==2.2.3
matplotlib==3.9.2
zarr==2.18.2
numcodecs==0.13.1
fsspec==2024.10.0
aiohttp==3.10.10
tensorstore==0.1.67
# Seung-lab image-analysis stack. All have linux-x86_64 wheels on PyPI.
# Enables 10–100× faster connected components, morphology, distance
# transform, skeletonization, and meshing vs scipy.ndimage/skimage for
# label volumes. Only available on the backend — Pyodide (browser) can't
# install these so the system prompt adapts accordingly.
connected-components-3d==3.18.0
fastmorph==1.2.0
fastremap==1.15.0
edt==2.4.1
kimimaro==5.8.1
zmesh==1.9.1
python-multipart==0.0.12
